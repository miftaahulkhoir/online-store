<?php
    $server = "localhost";
    $user = "root";
    $password = "";
    $database = "weshop";

    $koneksi = mysqli_connect($server, $user, $password, $database) or die("Koneksi ke database gagal");
    ?>
